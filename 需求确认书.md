# 法律知识问答助手 · 需求确认书 v1.1（已审核通过）

> 审核状态：✅ 用户已审核通过，Phase 1 MVP 完成，已升级为成熟前后端架构。

## 一、项目定位

基于 **RAG（检索增强生成）** 的法律知识问答 Web 应用：用户提问 → 系统从法律知识库检索相关内容 → LLM 结合检索结果生成带**引用溯源**的回答。覆盖四类用户：法律从业者、法学生、普通公众、企业内部使用者。

## 二、已确认需求

| 维度 | 结论 |
|---|---|
| 目标用户 | 法律从业者 / 法学生 / 普通公众 / 企业内部（全场景） |
| 知识库内容 | 法律法规 + 司法解释 + 裁判文书/案例 + 企业自有文档 |
| 数据来源 | 用户提供（`C:\Users\64517\Desktop\法律`，17,305 个文件，7.7GB） |
| 交付形态 | Web 网页应用 |
| 技术路线 | 云端 LLM API（DeepSeek）+ 本地中文嵌入（BGE） |
| 部署 | 先本机开发运行，部署方案后续再定 |
| 文档格式 | PDF（部分扫描件→Phase 2 OCR）、Word、TXT/Markdown、网页法规库 |
| 数据规模 | 最终目标万份级；Phase 1 先跑通小批量 |

## 三、核心功能（验收标准）

1. **引用溯源（强制）**：每个回答列出引用文档/法条，含出处；无法溯源时不编造，明确告知"未检索到相关依据"。
2. **多轮对话记忆**：支持追问、澄清，上下文随会话延续。
3. **文档上传与管理**：Web 界面上传 PDF/Word/TXT/MD 自动入库；扫描件自动走 OCR（Phase 2）。
4. **法规时效提示**：法条标注"现行有效/已修订/已废止"（Phase 2）。
5. **相似案例推荐**：回答下方附带相关案例推荐（Phase 2）。
6. **快慢分流**：常识性问题直接回答（快路径），具体法条走完整 RAG（慢路径）；拿不准一律走慢路径。
7. **免责声明**：界面常驻"AI 生成内容仅供参考，不构成正式法律意见"。

## 四、分阶段交付计划

- **Phase 1（已完成 ✅）**：MVP 核心链路 + 成熟前后端架构升级
  - RAG 核心：docx/pdf 解析切块、本地 BGE 中文嵌入（512 维）、轻量向量库、DeepSeek 生成、引用溯源强制、多轮对话、快慢分流
  - 后端：FastAPI 分层架构（api/services/repositories/schemas/core）+ 统一错误处理 + 结构化日志 + pydantic 校验 + 测试
  - 前端：Vite + React 18 + TypeScript 工程（组件化、类型安全、API 客户端、构建产物由后端托管）
  - 检索增强：条款号精确匹配（感知法律名，解决跨法律同名条款歧义）
- **Phase 2（已完成 ✅）**：成熟化功能扩展
  - 文档上传管理：Web 上传 docx/pdf/txt/md → 异步后台入库，可查看/删除，中文文件名编码自动修复
  - 扫描件 OCR：RapidOCR 接入 pdf 解析，图片型 PDF 自动识别（实测扫描件 3 页全识别）
  - 法规时效：规则引擎检测效力状态（现行有效/已修订/已废止/部分失效），存量数据回填，回答中提示"已被修订"
  - 相似案例推荐：检索附带司法解释/案例类推荐列表
  - 测试扩充至 39 个用例
- **Phase 3（进行中）**：
  - ✅ **3a 产品化改造（已完成）**：用户系统（注册/登录/登出、PBKDF2 密码哈希、Bearer Token 鉴权、会话按用户隔离）；会话历史管理（侧边栏列表、自动标题、切换/新建/删除）；知识库管理侧边栏（上传/列表/删除/统计）；全新视觉设计（专业配色、卡片化布局、深色侧边栏）
  - ⬜ 3b：万份级向量库（Milvus/Qdrant）、网页法规库采集、企业文档权限与部署。

## 五、风险与合规备忘

- 裁判文书类数据存在平台访问限制与合规风险，Phase 2/3 用合规替代方案；
- 万份级向量化与 API 调用有持续成本，规模化前先给估算；
- 企业敏感文档若上公网需补鉴权与数据安全设计；
- **API Key 安全**：只存在于后端 .env，绝不进入前端/代码库；发布时用服务端代发或 BYOK 模式。
